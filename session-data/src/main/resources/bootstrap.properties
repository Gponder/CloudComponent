spring.application.name=session-data
# config client
spring.cloud.config.label=master
spring.cloud.config.profile=dev
spring.cloud.config.uri=http://localhost:8093/
# 需要启动 eu-server config-server redis
# config 信息必须配置在bootstrap中 server中的配置会覆盖其他配置